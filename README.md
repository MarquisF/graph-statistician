# An SPA website url monitor.
This monitor uses the graph data structure to store the relations between urls of an SPA web application.

react.js, koa2, mongodb, socket.io

Still under development...
